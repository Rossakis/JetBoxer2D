namespace Super_Duper_Shooter.Classes.InputSystem.Base;

public class BaseInputAction
{
    //For each InputAction (e.g. MoveHorizontal) exists a dictionary, where for each input device (keyboard, mouse, etc...)
    //exists an Enum list containing all the inputs that can trigger the input action, with a
    //a list for each device (e.g. {Keys.A, Keys.Left} for Keyboard)
}